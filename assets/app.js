/**
 * 
 */
import { createRoot } from 'react-dom/client';

const modeleNode = document.getElementById('modele');
const rootModele = createRoot(modeleNode);

const valeurNode = document.getElementById('valeur');
const rootValeur = createRoot(valeurNode);